#include <stdio.h>
int main()
{
    int a[10],sum=0;
    int p[10];
    p[10]=&a[10];
    for(int i=0;i<10;i++)
    {
        printf("enter the number : ");
        scanf("%d",&a[i]);
        p[i]=a[i];
        sum=sum+a[i];
    }
    printf("%d",sum);
}
