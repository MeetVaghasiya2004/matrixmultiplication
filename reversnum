// Online C compiler to run C program online
#include <stdio.h>

int recur(int n)
{
    int x;
    if(n==0)
    {
        return 0;
    }
    x=n%10;
    printf("%d",x);
    recur(n/10);
}

int main() 
{
    int n;
    printf("enter the number : ");
    scanf("%d",&n);
    recur(n);
}
